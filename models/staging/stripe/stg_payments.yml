version: 2

models:
  - name: stg_payments
    columns: 
      - name: payment_method
        tests:
          - not_null
          - accepted_values:
              values:
                - credit_card
                - coupon
                - bank_transfer
                - gift_card